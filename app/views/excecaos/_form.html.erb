<%= form_with(model: excecao, local: true) do |form| %>
  <% if excecao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(excecao.errors.count, "error") %> prohibited this excecao from being saved:</h2>

      <ul>
      <% excecao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data %>
    <%= form.date_select :data %>
  </div>

  <div class="field">
    <%= form.label :hora %>
    <%= form.datetime_select :hora %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
